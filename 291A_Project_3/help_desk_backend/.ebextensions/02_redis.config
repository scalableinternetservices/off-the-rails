packages:
  yum:
    redis: []

services:
  sysvinit:
    redis:
      enabled: true
      ensureRunning: true

commands:
  01_start_redis:
    command: "sudo service redis start"
    ignoreErrors: true