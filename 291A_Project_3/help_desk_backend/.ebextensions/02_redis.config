packages:
  dnf:
    redis: []

services:
  sysvinit:
    redis:
      enabled: true
      ensureRunning: true

commands:
  01_start_redis:
    command: "sudo systemctl enable redis && sudo systemctl start redis"
    ignoreErrors: false